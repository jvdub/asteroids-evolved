# TASK-04: Options UX and Binding Polish

## Objective
Polish the options screen interaction model (bind buttons + key capture) to feel intentional and user-friendly.

## Scope
- Improve options table visual layout and readability.
- Add clear capture-state affordance for active bind button.
- Keep auto-save-on-bind behavior and local persistence flow.

## Deliverables
- Updated `style/main.css` options-specific polish.
- Minimal `scripts/screens.js` updates for capture-state visuals/text (if needed).

## Acceptance Criteria
- Bind flow is clear and low-friction.
- No free-text key entry is reintroduced.
- `npm run build` and `npm run check:static-arch` pass.
