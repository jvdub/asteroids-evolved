Before running the website, make sure you have all the correct dependencies by running `npm update` or `npm install`
To run the website, run the `app.js` file using node. i.e. `hotnode ./app.js`
This will start the asteroids game on port 3000 (http://localhost:3000/).